---
import Accordion from './Accordion.astro';

export interface Props {}
---

<div class="drug-content-wrapper">
  <!-- Overview (always open first) -->
  <Accordion id="overview" title="Overview" variant="primary" isOpen={true}>
    <div id="overview-content" class="accordion-content-section">
      <!-- Overview, Usage, Alternate Names will go here -->
    </div>
  </Accordion>

  <!-- Medical Information -->
  <Accordion id="medical-info" title="Medical Information" variant="default">
    <div id="medical-info-content" class="accordion-content-section">
      <!-- How It Works, Pharmacology, Mechanism -->
    </div>
  </Accordion>

  <!-- Dosage -->
  <Accordion id="dosage" title="Dosage Information" variant="default">
    <div id="dosage-content" class="accordion-content-section">
      <!-- Standard dose, pediatric dose, adjustments -->
    </div>
  </Accordion>

  <!-- Side Effects -->
  <Accordion id="side-effects" title="Side Effects" variant="default">
    <div id="side-effects-content" class="accordion-content-section">
      <!-- Common, serious, long-term effects -->
    </div>
  </Accordion>

  <!-- Safety -->
  <Accordion id="safety" title="Safety Information" variant="default">
    <div id="safety-content" class="accordion-content-section">
      <!-- Contraindications, Drug Interactions, Pregnancy & Breastfeeding -->
    </div>
  </Accordion>

  <!-- Reference -->
  <Accordion id="reference" title="Reference Information" variant="default">
    <div id="reference-content" class="accordion-content-section">
      <!-- Drug Profile Summary, Precautions, Monitoring Parameters, Popular Combinations -->
    </div>
  </Accordion>

  <!-- FAQs -->
  <Accordion id="faqs" title="Frequently Asked Questions" variant="default">
    <div id="faqs-content" class="accordion-content-section">
      <!-- FAQs -->
    </div>
  </Accordion>

  <!-- Hidden container -->
  <div id="original-content" style="display: none;">
    <slot />
  </div>
</div>

<style>
  .drug-content-wrapper {
    margin: 24px 0;
  }

  .accordion-content-section :global(h2:first-child) {
    margin-top: 0;
  }

  .accordion-content-section :global(h2) {
    color: #1e293b;
    font-weight: 600;
    margin: 32px 0 16px;
    font-size: 1.5rem;
    padding-bottom: 8px;
    border-bottom: 2px solid rgba(99, 102, 241, 0.1);
  }

  .accordion-content-section :global(h3) {
    color: #374151;
    font-weight: 600;
    margin: 24px 0 12px;
    font-size: 1.25rem;
  }

  .accordion-content-section :global(h4) {
    color: #4b5563;
    font-weight: 600;
    margin: 20px 0 10px;
    font-size: 1.1rem;
  }

  .accordion-content-section :global(p) {
    margin: 0 0 16px;
    line-height: 1.6;
  }

  .accordion-content-section :global(ul),
  .accordion-content-section :global(ol) {
    margin: 0 0 16px;
    padding-left: 24px;
  }

  .accordion-content-section :global(li) {
    margin-bottom: 8px;
  }

  .accordion-content-section :global(strong) {
    color: #1e293b;
    font-weight: 600;
  }

  .accordion-content-section :global(table) {
    width: 100%;
    border-collapse: collapse;
    margin: 24px 0;
    border: 1px solid rgba(226,232,240,0.8);
    border-radius: 8px;
    overflow: hidden;
  }

  .accordion-content-section :global(th),
  .accordion-content-section :global(td) {
    padding: 12px 16px;
    text-align: left;
    border-bottom: 1px solid rgba(226,232,240,0.6);
  }

  .accordion-content-section :global(th) {
    background: rgba(248,250,252,0.8);
    font-weight: 600;
    color: #1e293b;
  }

  .accordion-content-section :global(blockquote) {
    border-left: 4px solid rgba(99,102,241,0.3);
    padding: 16px 24px;
    margin: 24px 0;
    background: rgba(248,250,252,0.5);
    border-radius: 0 8px 8px 0;
    font-style: italic;
    color: #4b5563;
  }

  @media (max-width: 768px) {
    .drug-content-wrapper {
      margin: 16px 0;
    }
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    organizeContentIntoAccordions();
  });

  function organizeContentIntoAccordions() {
    const originalContent = document.getElementById('original-content');
    if (!originalContent) return;

    const h2Elements = originalContent.querySelectorAll('h2');
    const sections = [];

    h2Elements.forEach((h2, index) => {
      const section = { heading: h2, content: [] };
      let nextSibling = h2.nextElementSibling;
      const nextH2 = h2Elements[index + 1];

      while (nextSibling && nextSibling !== nextH2) {
        section.content.push(nextSibling);
        nextSibling = nextSibling.nextElementSibling;
      }

      sections.push(section);
    });

    // Map new sections
    const sectionMapping = {
      'Usage': 'overview-content',
      'Alternate Names': 'overview-content',
      'How It Works': 'medical-info-content',
      'Pharmacological Classification': 'medical-info-content',
      'Dosage': 'dosage-content',
      'Side Effects': 'side-effects-content',
      'Contraindications': 'safety-content',
      'Drug Interactions': 'safety-content',
      'Pregnancy and Breastfeeding': 'safety-content',
      'Precautions': 'reference-content',
      'Monitoring Parameters': 'reference-content',
      'Drug Profile Summary': 'reference-content',
      'Popular Combinations': 'reference-content',
      'FAQs (Frequently Asked Questions)': 'faqs-content',
      'FAQs': 'faqs-content'
    };

    sections.forEach(section => {
      const headingText = section.heading.textContent.trim();
      const targetId = sectionMapping[headingText];
      if (targetId) {
        const targetContainer = document.getElementById(targetId);
        if (targetContainer) {
          targetContainer.appendChild(section.heading.cloneNode(true));
          section.content.forEach(el => targetContainer.appendChild(el.cloneNode(true)));
        }
      }
    });

    // Hide empty accordions
    Object.values(sectionMapping).forEach(containerId => {
      const container = document.getElementById(containerId);
      if (container && container.children.length === 0) {
        const accordion = container.closest('.accordion');
        if (accordion) accordion.style.display = 'none';
      }
    });

    originalContent.style.display = 'none';
  }
</script>
